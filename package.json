{
  "name": "remedy-pdf-desktop",
  "private": true,
  "version": "1.0.0",
  "description": "Remedy PDF Desktop — standalone native document accessibility remediation app",
  "license": "MIT",
  "scripts": {
    "build": "npm run web:build",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "web:dev": "cd web && npm run dev",
    "web:build": "cd web && npm run build",
    "dist:mac": "bash scripts/release_macos.sh",
    "dist:mac:dry": "bash scripts/release_macos.sh --dry-run"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
